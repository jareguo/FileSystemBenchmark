var server = require("./server");
var router = require("./router");
var requestHandlers = require("./requestHandlers");

var handle = {}
handle["/navigate"] = requestHandlers.navigate;
handle["/navigate_sync"] = requestHandlers.navigateSync;
handle["/open"] = requestHandlers.open;
handle["/open_sync"] = requestHandlers.openSync;
handle["/get_drives"] = requestHandlers.getDrives;
//handle["/"] = handle["/navigate"];

server.start(router.route, handle);